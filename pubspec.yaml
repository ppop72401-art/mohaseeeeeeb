name: modern_accounting_system
description: A modern desktop accounting system.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # قاعدة البيانات للويندوز
  sqflite_common_ffi: ^2.3.0
  sqflite: ^2.3.0
  path: ^1.8.3
  # إدارة الحالة
  provider: ^6.0.5
  # خطوط عصرية
  google_fonts: ^5.0.0
  # أيقونات عصرية
  fluentui_system_icons: ^1.1.214

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
